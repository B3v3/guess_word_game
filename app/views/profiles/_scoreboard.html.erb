<ul class="list-group">
<%= paginate @users %>
  <% @users.each do |user| %>
    <li class="list-group-item text-center">
      <%= link_to "#{user.name} - #{user.score} points", profile_path(user) %>
    </li>
  <% end %>
<%= paginate @users %>
</ul>
