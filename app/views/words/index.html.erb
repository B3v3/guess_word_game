<div class="jumbotron text-center">
  <%= link_to "Create new Word", new_word_path,
                                 class: 'btn btn-info btn-lg btn-block' %>

  <%= render @words if @words.any? %>
</div>
