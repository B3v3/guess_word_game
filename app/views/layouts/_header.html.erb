<nav class="navbar navbar-expand-lg navbar-light navbar-inverse">
  <div class="container">
    <span class="navbar-text">
      <%= link_to "Guessing Game", root_path, class: 'h2 text-dark' %>
    </span>
    <div class="navbar-brand navbar-right">
      <% if user_signed_in? %>
        <%= link_to '| Scoreboard |', profiles_path,
          class: 'h3 text-dark' %>

        <%= link_to '| Profile |', profile_path(current_user),
            class: 'h3 text-dark' %>

        <%= link_to "| Log out |", destroy_user_session_path,
            data: {:confirm => 'Are you sure?'}, :method => :delete,
            class: 'h3 text-dark' %>
      <% else %>
        <%= link_to "Log in", new_user_session_path, class: 'h3 text-dark' %>
      <% end %>
    </div>
 </div>
</nav>
