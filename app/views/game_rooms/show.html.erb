<div class="jumbotron">
  <div class='text-center'>
    <h1> <%= @game_room.full_name %></h1>
    <p> <%= @game_room.description %></p>
  </div>

  <h2> Guesses: </h2>
  <%= render 'guesses/guess_form' %>

  <%= render @guesses if @guesses.any? %>

  <%= link_to 'Return', game_rooms_path, class: 'btn btn-info btn-lg btn-block' %>
<div>
